using Xunit;

namespace Toy.Pubsub.Tests
{
    public class UnitTest1
    {
        [Fact]
        public void Test1()
        {
            // TODO : (dh) Test 추가예정
            //bool isSubscripbe = false;

            //PubSubService pubSubService = new PubSubService();

            //var pubsubName = "Name";
            //pubSubService.Subscribe(pubsubName, (string value) =>
            //{
            //    var dto = JsonSerializer.Deserialize<SampleDTO>(value);

            //    if (dto.Value == 1) 
            //    {
            //        isSubscripbe = true;
            //    }
            //});

            //var dto = new SampleDTO()
            //{
            //    Value = 1
            //};

            //var jsonString = JsonSerializer.Serialize(dto);
            //pubSubService.Publish(pubsubName, jsonString);

            //Assert.True(isSubscripbe);
        }
    }
}
